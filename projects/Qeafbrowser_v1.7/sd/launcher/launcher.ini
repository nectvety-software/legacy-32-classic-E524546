; Qeafbrowser launcher config — copy vao the SD tai /launcher/launcher.ini
; theme = ten file .vqeaf trong LittleFS /launcher/themes/ (khong gom duoi)
; brightness = 0..255 (LED PWM)
theme=retrogo_dark
brightness=200
